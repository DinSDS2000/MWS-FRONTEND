name: flutter_epihhinventory
description: A mobile inventory application interface with Epicor.
version: 2.1.7

environment:
   sdk: ">=3.0.0 <4.0.0"
  # flutter: ^1.9.1

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.1
  # get_version: ^0.0.8+5
  flutter_whatsnew: ^1.0.5+2
  # native_widgets: ^1.0.1
  shared_preferences: ^2.5.2
  local_auth: ^2.3.0
  cupertino_icons: ^1.0.8
  # persist_theme: ^0.0.3+6
  scoped_model: ^2.0.0
  http: ^1.3.0
  json_annotation: ^4.9.0
  barcode_scan2: ^4.5.0
  global_configuration: ^2.0.0
  decimal: ^3.0.0
  modal_progress_hud_nsn: ^0.5.0
  intl: ^0.19.0
  flutter_sticky_header: "^0.7.0"
  flutter_udid: ^4.0.0
  image_picker: ^1.2.0
  path_provider: ^2.1.5
  # imei_plugin: ^1.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  json_serializable: ^6.9.4

flutter:
  uses-material-design: true

  assets:
   - assets/images/login_contact.png
   - CHANGELOG.md
